{
  "name": "react-vite-tailwind-express-starter-2",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently \"cd client && npm install && npm run build && npm run dev\" \"node server/index.js\"",
    "start": "npm run build && node server/index.js",
    "build": "cd client && npm install --include=dev && npm run build && cd .. && npm install"
  },
  "description": "A fullstack starter template using React, Vite, Tailwind CSS, and Express",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.1",
    "vite": "^6.0.11"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
